version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - mvn install
  post_build:
    commands:
      - echo Hello world
artifacts:
  files:
    - target/messageUtil-1.0.jar
